## What this changes

<!-- One or two sentences. -->

## Checklist

- [ ] `npm test` passes
- [ ] `npm run check` passes
- [ ] The extension still only reads: no clicks, no drags, no patched BGA functions
- [ ] Anything it cannot compute is reported in the panel rather than scored as zero
- [ ] Both `ru` and `en` strings updated, if the UI changed
